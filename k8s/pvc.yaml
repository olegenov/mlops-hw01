apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dvc-cache-pvc
  namespace: mlops-hw
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datasets-pvc
  namespace: mlops-hw
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
